{
  "devDependencies": {
    "@types/three": "^0.164.1",
    "gh-pages": "^6.1.1",
    "vite": "^5.2.11"
  },
  "dependencies": {
    "three": "^0.164.1"
  },
  "scripts": {
    "dev": "vite",
    "test": "echo \"Error no test specified\" && exit 1",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d ./dist"
  }
}
